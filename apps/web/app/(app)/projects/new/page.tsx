import { OnboardingFlow } from "@/components/onboarding/onboarding-flow";

export const metadata = { title: "Onboard a business — TrendRadar" };

export default function NewProjectPage() {
  return <OnboardingFlow />;
}
